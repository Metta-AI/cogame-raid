version     = "0.1.0"
author      = "daveey"
description = "Raid: five strangers versus SMELTER-9, a fully scripted foundry boss with authored phases, telegraphed floor mechanics, adds, an interruptible cast and a hard enrage timer."
license     = "MIT"

srcDir = "src"

requires "nim >= 2.2.4"
requires "bitworld >= 0.1.0"
requires "mummy >= 0.4.7"
requires "curly >= 1.1.1"
requires "whisky"
